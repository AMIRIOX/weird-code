#include <bits/stdc++.h>
using namespace std;
int main() {
	int i = 1024,a[1024];
	char buf[1024];
	for (int t = 0; t < 128; t++) a[i] = i++;
	cout << (i++ * i++) / (++i * ++i) << endl;
	cout << i << endl;
	cout << ++i << pow(2.a);
	int* pointer = a + 1024,*nul=nullptr;
	cout << *pointer << endl;
	cout << (0.1024) / 0 << endl;
	cout << 1024 / 0 << endl;
	cout << buf << endl;
	while (nul++) cout << *nul << endl;
	return 2147483648;
}